# XiPod runtime dependencies (Python 3.13)
# audioop-lts is required by pydub on Python 3.13+ (audioop was removed
# from the standard library).
numpy==2.4.2
pydub==0.25.1
audioop-lts==0.2.2
PyAudio==0.2.14
pedalboard==0.9.22
# PyAV bundles ffmpeg's libraries in the wheel, so users don't need ffmpeg on
# their PATH for .mp3/.ogg/etc. All file loading goes through src/decode.py;
# pydub is still used for slicing, crossfades and the FX chain.
av==18.0.0
# Essentials only — NOT the full PySide6 meta-package. PySide6-Addons is what
# drags in QtWebEngine (~360 MB of Chromium), and nothing in this app needs a
# browser engine.
PySide6-Essentials==6.11.0
