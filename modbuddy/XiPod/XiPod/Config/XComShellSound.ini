[MusicModdingSystem.MMS_UISL_UIShell]
; Anarchy Radio FM ships this section EMPTY on purpose — see the note in
; XComStrategySound.ini. The desktop app writes the real ShellCues entry into
; the game's user config directory.
