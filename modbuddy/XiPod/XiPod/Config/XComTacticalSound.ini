[MusicModdingSystem.MMS_XComTacticalSoundManager]
; Anarchy Radio FM ships this section EMPTY on purpose — see the note in
; XComStrategySound.ini. The desktop app writes the real entries into the
; game's user config directory.
;
; Shipping CombatDefs/ExploreDefs here as well as there put two identical defs
; into MMS's pool: real logs show "found XiPod_Silence_Combat" twice at the
; start of every mission.
